{
  "name": "@socialflow/logger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit false --outDir dist",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "pino": "^9.7.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
